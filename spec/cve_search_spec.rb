require 'spec_helper'

describe CveSearch do
  it 'has a version number' do
    expect(CveSearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
